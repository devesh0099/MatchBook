# Local state. It holds the assumed-role session and every resource attribute;
# for the real event it would hold the Postgres password too.
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
terraform.tfvars
crash.log
